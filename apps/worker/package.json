{
  "name": "@vpn-platform/worker",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "predev": "pnpm --filter @vpn-platform/contracts build",
    "dev": "node --env-file-if-exists=../../.env --import tsx --watch src/main.ts",
    "build": "tsc -p tsconfig.build.json",
    "prestart": "pnpm --filter @vpn-platform/contracts build",
    "start": "node --env-file-if-exists=../../.env dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "pnpm --filter @vpn-platform/contracts build",
    "test": "vitest run --exclude src/outbox-publisher.integration.test.ts",
    "pretest:integration": "pnpm --filter @vpn-platform/contracts build",
    "test:integration": "node --env-file-if-exists=../../.env node_modules/vitest/vitest.mjs run src/outbox-publisher.integration.test.ts"
  },
  "dependencies": {
    "@prisma/client": "6.19.3",
    "@vpn-platform/contracts": "workspace:*",
    "bullmq": "^5.61.0",
    "pino": "^9.9.4",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "tsx": "^4.20.6",
    "vitest": "^3.2.4"
  }
}
